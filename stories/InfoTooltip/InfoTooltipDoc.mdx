import { Props, Story, Preview } from '@storybook/addon-docs/blocks'
import InfoTooltip from '@/components/InfoTooltip/InfoTooltip.vue';
import './../GlobalStorybook.scss'

# Info Tooltip Docs

## Overview

<b>'info-tooltip'</b> is used as a brief, informative message box when hovering over an item on desktop view or through touch on mobile view. The text prop is used to pass additional informational text. The text container has a grey outline and white background. 

## Props
<Props of={InfoTooltip} />

## Example
<Preview><Story id="infotooltip--info-tooltip" /></Preview>
