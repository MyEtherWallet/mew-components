import { Props, Story, Preview } from '@storybook/addon-docs/blocks'
import MewAlert from '@/components/MewAlert/MewAlert.vue';
import './../GlobalStorybook.scss'

# MEW Alert
`<mew-alert />`

## Overview

The mew-alert component is used to display different types of alerts, such as: error, warning, success, info. The type of alert is determined by the **theme** prop.

## Props
<Props of={MewAlert} />
<Preview><Story id="mewalert--mew-alert" /></Preview>


